"""Simplified script package: only two commands are exposed.

Modules:
- set_exif_from_metadata: Fill EXIF dates from Google Photos JSON metadata.
- move_with_exif: Move files that already have EXIF timestamps.
"""

